import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'VERA NOIR — Demo | Foundry AI',
  description: 'VERA NOIR fashion boutique demo — converted from html-originals/vera-noir-boutique.html',
}

export default function BoutiqueRopaLayout({ children }: { children: React.ReactNode }) {
  return children
}
