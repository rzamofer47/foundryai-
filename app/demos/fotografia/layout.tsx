import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Julian Cross — Photography Demo | Foundry AI',
  description: 'Julian Cross Photography demo — converted from html-originals/julian-cross-photography-styles.css',
}

export default function FotografiaLayout({ children }: { children: React.ReactNode }) {
  return children
}
