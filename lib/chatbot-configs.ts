/** Shared chatbot / demo brand tokens for client demos. */

export type ChatbotDemoConfig = {
  siteName: string
  primaryColor: string
}

/** Manicure editorial demo — used by `/demos/manicure` chat + accents */
export const manicureConfig: ChatbotDemoConfig = {
  siteName: 'Ligne Nude',
  primaryColor: '#9B7B8C',
}
